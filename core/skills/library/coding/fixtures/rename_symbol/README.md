# rename_symbol

`util.py` defines one helper; `greet.py`, `report.py` and the test call it. There is deliberately no `.judais-lobi.yml`, so `verify test` falls back to the default `{python} -m pytest`.
